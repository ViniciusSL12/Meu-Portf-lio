Next.js Portfolio - Vinicius S. Lima (v2)

Alterações:
- Projects page now fetches exactly three specific repos via GitHub API.
- Theme toggle moved to header (top-right).
- Experiencia page merges academic and professional sections with example text.

Run:
1. npm install
2. (optional) create .env.local with GITHUB_TOKEN=your_token
3. npm run dev
